name: Test and Build
on:
  pull_request:
    branches:
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: '21'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm install

      # for when we have tests
      # - name: Run tests
      #  run: npm test

      - name: Build
        env:
          DATABASE_URL: ${{ secrets.test.DATABASE_URL }}
          NEXTAUTH_SECRET: ${{ secrets.test.NEXTAUTH_SECRET }}
          GITHUB_CLIENT_ID: ${{ secrets.test.NEXTAUTH_GITHUB_CLIENT_ID }}
          GITHUB_CLIENT_SECRET: ${{ secrets.test.NEXTAUTH_GITHUB_CLIENT_SECRET }}
          GOOGLE_CLIENT_ID: ${{ secrets.test.NEXTAUTH_GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.test.NEXTAUTH_GOOGLE_CLIENT_SECRET }}
        run: npm run build